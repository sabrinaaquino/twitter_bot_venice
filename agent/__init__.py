"""LlamaIndex ReAct agent package (Milestone 1 — thin vertical slice).

Additive and flag-gated (Config.USE_AGENT). The deterministic safety guardrail in
agent.guardrails wraps the agent so the LLM can never post around the screens.
"""
