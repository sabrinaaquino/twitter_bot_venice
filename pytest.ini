[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Live tests hit the Venice API and are excluded from the default run.
# Run them explicitly with:  pytest -m live
addopts = -m "not live"

markers =
    live: hits the live Venice API (requires VENICE_API_KEY); excluded by default
