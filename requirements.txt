tweepy
requests
python-dotenv

# Agent (Milestone 1) — LlamaIndex ReAct agent stack
llama-index-core
llama-index-llms-openai-like
llama-index-embeddings-openai-like
# Optional, dev-only local embedding fallback (the Venice account currently
# returns HTTP 402, so live embeddings fail). Pulls sentence-transformers/torch
# (heavy) — install only for the local CLI demo:
#   pip install llama-index-embeddings-huggingface
